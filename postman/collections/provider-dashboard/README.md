# Car Hero - Provider Dashboard API

Direct source collection for the provider dashboard. Edit this collection when adding provider dashboard endpoints.

- API: `http://localhost:3001/api/v1`
- WebSocket: `http://localhost:3001/ws`
- Requests: 65

Validate before pushing:

```text
npm run postman:validate
```
